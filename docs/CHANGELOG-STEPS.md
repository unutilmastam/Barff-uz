# QADAMLAR JURNALI

> `ROADMAP.md` §4: har tugallangan qadam uchun bitta qator.
>
> Format: `S00 | 2026-08-27 | bootstrap monorepo | PR #1 | notes: ...`

```
S00 | 2026-09-21 | bootstrap monorepo | branch step/S00-repo-bootstrap | notes: pnpm 10 + turbo 2; minio lokal S3 uchun; `docker compose up` bu muhitda sinalmagan (daemon yo'q), faqat `docker compose config` bilan sintaksis tekshirildi
S01 | 2026-09-21 | shared packages skeleton | branch step/S01-shared-packages | notes: @barff/{types,utils,validation,config}; CommonJS build (Next + Nest ikkalasi o'qiy oladi); 53 ta vitest testi; tailwind theme.css CSS-first preset, yashil rang hali REPLACE_WITH_REAL_DATA (Q2)
S02 | 2026-09-22 | nestjs api skeleton | branch step/S02-api-skeleton | notes: validatsiya class-validator emas, Zod orqali (S01 sxemalari server va klientda BITTA manba); readiness/liveness ajratilgan; rate limit instansiya xotirasida (Q19); API_TRUST_PROXY_HOPS qo'shildi (Q20); consistent-type-imports API'da o'chirilgan — u Nest DI metadata'sini buzadi
S03 | 2026-09-22 | prisma core schema and seed | branch step/S03-prisma-core-schema | notes: sxema ildizdagi prisma/ da, klient packages/db/generated ga chiqadi (commit qilinmaydi); parollar argon2id, seed'da standart parol YO'Q — production'da SEED_ADMIN_PASSWORD majburiy; ruxsatlar katalogi @barff/types da yagona manba; VISITOR bazaga yozilmaydi; haqiqiy PostgreSQL 16 bilan migrate+seed noldan ikki marta sinaldi
S04 | 2026-09-22 | auth and server-side rbac | branch step/S04-auth-rbac | notes: endpoint'lar standart holatda YOPIQ (@Public() bilan ochiladi); ruxsatlar token ichidan emas, bazadan o'qiladi — rol o'zgarishi darhol kuchga kiradi; refresh rotation + qayta ishlatishni aniqlash (butun oila bekor bo'ladi); HttpOnly cookie; kirish urinishlari email va IP bo'yicha alohida cheklanadi (IP chegarasi NAT uchun ancha yuqori — Q21); 84 ta API testi, parol/token loglarga tushmasligi chiqish oqimini ushlab tekshiriladi
S05 | 2026-09-22 | ci pipeline and docker images | branch step/S05-ci-docker | notes: Dockerfile bookworm-slim (alpine emas — Prisma engine target), non-root; CI'da build seed'dan OLDIN (turbo Prisma klientini yaratadi, seed @barff/types dist'ini o'qiydi); deploy OIDC orqali, uzoq muddatli AWS kalitlarisiz; Next.js app Dockerfile'lari S06 da; tasvir lokalda qurilmadi (bu muhitda docker daemon yo'q), lekin CI'da muvaffaqiyatli qurildi va tekshirildi: non-root, native modullar yuklanadi, bazasiz ham ko'tariladi; CI 4 ta haqiqiy xatoni topdi — prettierignore, pnpm TTY, turbo env passthrough, modul qidirish katalogi
S06 | 2026-09-22 | web app shell and theme tokens | branch step/S06-web-shell | notes: PHASE 0 GATE; i18n kutubxonasiz — o'zbekcha fayl manba tip, tarjima qilinmagan kalit kompilyatsiyada ushlanadi; axe 0 buzilish, Lighthouse a11y 100/100; --color-fg-subtle AA dan o'tmagani aniqlanib tuzatildi (3.65 -> 5.14) va kontrast testi qo'shildi; 320px da gorizontal skroll yo'q; web Dockerfile (standalone, non-root) CI'ga ulandi
S07 | 2026-09-22 | barff design system | branch step/S07-design-system | notes: 14 primitiv + 5 yuza; Radix murakkab interaktivlar uchun (fokus tutqichi, Escape, fokus qaytishi), qolgani qo'lda; /uz/dev/ui ko'rish sahifasi page.dev.tsx orqali production'dan chiqarilgan (NODE_ENV tekshiruvi ishonchsiz edi — 200 qaytarardi); danger tugmasidagi oq matn AA dan o'tmagani aniqlanib qora matnga o'tkazildi va kontrast testiga tugma juftliklari qo'shildi; axe: gallereya va ochiq dialogda 0 buzilish
S08 | 2026-09-22 | media pipeline with s3 adapter | branch step/S08-media-storage | notes: tur imzo baytlari bo'yicha aniqlanadi (kengaytma/Content-Type emas), SVG rad etiladi; standart PRIVATE, ACL hech qachon qo'yilmaydi; kalit id+aniqlangan turdan; sharp bilan WebP+AVIF variantlar va blur o'rindosh, EXIF ko'chirilmaydi; MinIO bu muhitda ishga tushirilmadi (egress bloklangan) — S3 adapteri yuborilayotgan buyruqlar bo'yicha tekshirildi
S09 | 2026-09-22 | products domain | branch step/S09-products | notes: 6 model (kategoriya, mahsulot, variant, rasm, hujjat, narx); ommaviy API faqat faol+o'chirilmagan (shart servisda bitta joyda); ko'p tilli maydonlar to'liq obyekt sifatida qaytadi; narx butun tiyinda, kasr rad etiladi; slug/sku takrorlansa 409; yumshoq o'chirish; seed'da [MOCK] mahsulotlar; createZodDto tip xulosasi tuzatildi (.extend/.partial da unknown ga tushardi) va `defined()` yordamchisi qo'shildi
S10 | 2026-09-22 | content domain | branch step/S10-content | notes: 7 model (yangilik, sertifikat, galereya, hujjat, bosqich, bosh sahifa bo'limi, SEO); nashr sharti bitta joyda (content.filters.ts); yangi yozuv standart holatda DRAFT; kelajakdagi publishedAt bilan maqola hali ko'rinmaydi (rejalashtirilgan nashr); 8 bosqich seed'da, lekin DRAFT — tavsiflar REPLACE_WITH_REAL_DATA
S11 | 2026-09-22 | public api with caching | branch step/S11-public-api | notes: kesh bekor qilish versiya hisoblagichi orqali (SCAN+DEL emas — sekin va atomar emas); TTL majburiy; Redis xatolari yutiladi (kesh tezlik uchun, to'g'rilik uchun emas); javoblar public.mappers.ts da aniq shakllantiriladi — ichki maydonlar chiqmaydi; ETag + Cache-Control, mos kelsa 304 va tana yuborilmaydi
S12 | 2026-09-22 | home company and product pages | branch step/S12-web-pages | notes: ommaviy javob shakllari @barff/types ga ko'chdi — API va web bitta shartnomani baham ko'radi; kontent endpoint'lari xom Prisma qatorini qaytarayotgani aniqlandi (deletedAt, updatedAt, S3 `key` chiqib ketardi) — mapper'lar qo'shildi va sizib chiqishga qarshi e2e test yozildi; rasmlar `<picture>` orqali (quvur AVIF+WebP ni 320-1600px da allaqachon tayyorlaydi — next/image ustidan qayta optimallashtirish va host oq ro'yxati kerak bo'lardi); LCP — matn (H1/hero), 64-108ms; 360px da gorizontal skroll yo'q, axe 0 buzilish, 6 sahifa x 2 o'lcham; statistika raqamlari O'YLAB TOPILMADI — MOCK belgisi bilan bo'sh; uch xato topildi va tuzatildi: (1) `limit=200` server validatsiyasidan o'tmay generateStaticParams jimgina bo'sh qaytarardi, (2) not-found ichida cookies() butun saytni dinamik qilib SSG'ni yo'q qilardi, (3) qurilmagan sahifaga havola Next prefetch'ini MANGU osiltirardi — routes.ts qo'shildi; ma'lum cheklov: dinamik sahifada notFound() status'ni 404 qila olmaydi (Next oqimi) — 404 sahifasi ko'rinadi, status 200; next.config NODE_ENV emas, phase bo'yicha qaror qiladi
S13 | 2026-09-23 | remaining public pages | branch step/S13-public-pages | notes: 10 marshrut (`/production`, `/quality`, `/partners`, `/news`, `/news/[slug]`, `/gallery`, `/catalog`, `/contact`, `/privacy`, `/terms`); navigatsiya YAGONA ro'yxatdan (`navigation.ts`) — sarlavha, pastki qism va sitemap bir manbadan, qurilmagan sahifa ro'yxatga tushmaydi; mobil menyu `Sheet` ustida (fokus tutqichi, Escape, havoladan keyin o'z-o'zidan yopiladi — tekshirildi); galereya ko'ruvchisi klaviatura bilan aylanadi, ochiq holatda ham axe 0 buzilish; S10 dagi `system_settings` bo'shlig'i yopildi (ommaviy o'qish + admin CRUD + audit) — `/contact` shusiz ishlamasdi, `isPublic` bo'lmagan sozlama chiqmasligi testda qoplangan; maqola matni ODDIY MATN sifatida chiziladi (`dangerouslySetInnerHTML` yo'q — saqlangan XSS yo'li yopiq); `/privacy` va `/terms` MATNI YOZILMADI — huquqiy majburiyat o'ylab topilmaydi, sahifa mavjud va `noindex`; `/contact` da `REPLACE_WITH_REAL_DATA` o'rindoshi KO'RSATILMAYDI (Q11); sitemap uchala tilni `alternates` bilan bog'laydi, robots standart holatda taqiq; tekshirildi: 42 sahifa aylanib chiqildi — 0 buzilgan havola, 15 sahifa x 2 o'lcham axe 0 buzilish, 360px da gorizontal skroll yo'q
S14 | 2026-09-23 | b2b leads and notification adapters | branch step/S14-leads | notes: `leads` + `lead_events` + `notifications` modellari; o'tish qoidalari @barff/types da (NEW->CONTACTED->... , CONVERTED dan keyin yo'l yo'q); himoya UCH qatlam — honeypot (`display:none`), IP bo'yicha soatiga 5 ta, va 30 daqiqalik takror oynasi (kalit normallashtirilgan telefon+kompaniya, ya'ni `+998 90 123-45-67` va `998901234567` bitta odam); takror bo'lsa ham javob BIR XIL (`{accepted:true}`) — aks holda javobdan kim ariza berganini bilish mumkin edi; bildirishnoma provayder adapteri orqali (Telegram + log/MOCK), xato YUTILADI va `notifications` jadvalida FAILED bo'lib qoladi — ariza bildirishnomaga bog'liq emas; manzillar sozlamadan (`notifications.lead`), `REPLACE_...` bo'lsa yuborilmaydi; forma React Hook Form + server bilan BITTA Zod sxemasi; bo'sh satrni `undefined` ga aylantiruvchi qatlam qo'shildi (`email: ''` ixtiyoriy maydonni majburiyga aylantirardi); UI primitivlarida ixtiyoriy xossalar `| undefined` bilan yozildi (exactOptionalPropertyTypes); brauzerda tekshirildi: bo'sh forma server so'roviga aylanmaydi, to'ldirilgani 1 ta so'rov va muvaffaqiyat xabari, axe 0 buzilish, 360px; tezlik chegarasi ALOHIDA e2e faylda tekshiriladi (asosiy to'plamda ThrottlerStorage almashtirilgan); S13 da xatolik bilan `dump.rdb` (lokal Redis snapshot) commit qilingan ekan — olib tashlandi va `.gitignore` ga qo'shildi
S15 | 2026-09-23 | i18n and seo | branch step/S15-i18n-seo | notes: metadata YAGONA `buildMetadata()` orqali — kanonik, hreflang (uz/ru/en + x-default), Open Graph, Twitter kartochkasi va robots bitta joyda, shuning uchun yangi sahifada ularning biri unutilmaydi; CMS yozuvi (`/seo?path=`) sahifaning o'z matnidan USTUN — uchala tilda tekshirildi; layout `title.template` beradi ('%s — BARFF'), bosh sahifa `absoluteTitle` bilan takrorlanishdan qochadi; tuzilgan ma'lumotlar: Organization, Product, NewsArticle, BreadcrumbList — `aggregateRating`, `review` va `offers` UMUMAN yo'q va bu testda qoplangan (soxta yulduzcha qidiruv natijasida yolg'on bo'lardi); qiymati yo'q kalit butunlay tushiriladi, bo'sh maydon berilmaydi; `check-hardcoded-strings.mjs` skripti JSX ichidagi tarjimasiz matnni topadi va `lint` ga ulandi — istisno `i18n-exempt` izohi bilan ko'rinib turadi; tarjima nusxasini ushlaydigan test qo'shildi (uz qiymati ru/en ga ko'chirilgan bo'lsa yiqiladi); ESLint v10 ENG YAQIN konfiguratsiyani oladi — shuning uchun `scripts/` uchun Node global'lari ildizga emas, umumiy presetga qo'shildi; brauzerda tekshirildi: 18 marshrut, har birida sarlavha/tavsif/kanonik/4 ta hreflang/OG/Twitter, bitta h1, alt'siz rasm yo'q, huquqiy hujjatlarda noindex; til almashtirish yo'lni saqlaydi
S16 | 2026-09-23 | gsap motion layer | branch step/S16-motion | notes: `motion/config.ts` — davomiylik va egri chiziq YAGONA manbada, CSS token'lari bilan mos (test bilan qo'riqlanadi); `Reveal` kontentni STANDART holatda KO'RSATADI va yashirishni faqat JavaScript ichida qo'yadi — CSS'ga `opacity:0` yozilsa, skript yuklanmaganda matn mangu ko'rinmay qolardi; GSAP va Lenis DINAMIK import (asosiy bundle 102 kB o'zgarmadi); harakat uch shart bilan yoqiladi: `prefers-reduced-motion` yo'q, qurilma kuchli, hidratsiya tugagan — standart javob YO'Q; Lenis qo'shimcha ravishda faqat `pointer: fine` da (sensorli ekranda brauzerning o'z skrolli qoladi); DoD brauzerda tekshirildi — JS o'chirilganda butun kontent ko'rinadi va 21 ta havola ishlaydi, `reduce` da animatsiya UMUMAN ishga tushmaydi, CLS=0; YO'L-YO'LAKAY topilgan S12 kamchiligi tuzatildi: `[locale]/loading.tsx` butun sahifa uchun Suspense chegarasi yaratib, o'rindosh->kontent almashinuvida har 10 yuklashning birida CLS=0.2278 berardi — o'lchov harakat yoqilgan va kamaytirilgan holatda BIR XIL chiqqani bu surilish animatsiyadan emasligini ko'rsatdi; o'rindosh faqat dinamik sahifalarga (`products/[slug]`, `news/[slug]`) ko'chirildi va CLS 30/30 o'lchovda 0 bo'ldi
S17 | 2026-09-23 | three.js hero scene | branch step/S17-hero-3d | notes: sahna KOD ichida quriladi (lathe geometriya) — tashqi `.glb` yo'q, chunki haqiqiy model BARFF dan kelishi kerak va o'ylab topilgan shishani fayl sifatida saqlash uni "haqiqiy" ko'rsatardi; drei ISHLATILMADI (undan faqat `Float` va `Environment` kerak bo'lardi — biri bir necha qator kod, ikkinchisi tashqi HDR fayl); `next/dynamic` + `ssr:false`, server HTML'da three/canvas UMUMAN yo'q va bosh sahifa bundle'i 157->160 kB; sahna uch shartda chizilmaydi va zaxira gradient qoladi: reduce, kuchsiz qurilma, WebGL yo'q; `transmission` material ATAYLAB ishlatilmadi (har kadrda sahnani qayta chizadi); zarrachalar soni ekran kengligiga qarab 140/60; o'lchandi: LCP = H1 @160ms (CANVAS emas), CLS=0, reduce va WebGL'siz holatda 0 ta canvas lekin sarlavha joyida, JS'siz server HTML toza; R3F global `JSX.IntrinsicElements` ni kengaytirgani uchun polimorf `as` komponentlarida xossalar kesishmasi `never` bo'lib qolardi — `Reveal`, `Link` va `GlassCard` `createElement` ga o'tkazildi; MA'LUM LOKAL CHEKLOV: S3 sozlanmagan muhitda xotiradagi adapter `memory://` manzil qaytaradi va brauzer uni yuklay olmaydi — galereya rasmlari faqat lokalda ko'rinmaydi, production'da S3 majburiy
S18 | 2026-09-23 | admin shell with rbac | branch step/S18-admin-shell | notes: `apps/admin` (3002-port) — kirish, sessiya, himoyalangan layout, rolga qarab menyu va jadval komponenti; sessiya SERVERDA `/auth/me` orqali o'qiladi, token `HttpOnly` cookie'da va JavaScript unga yetmaydi (brauzerda tekshirildi: `document.cookie` bo'sh); menyu ruxsatga qarab filtrlanadi, LEKIN bu kosmetika — DoD sifatida 23 ta e2e test yozildi: DRIVER 9 ta admin endpoint uchun 403, SALES sozlamalar va kontent yozish uchun 403, ADMIN hammasiga 200, tokensiz 401, soxta token 401; brauzerda ham tasdiqlandi — DRIVER `fetch` bilan to'g'ridan-to'g'ri chaqirsa ham 403; JIDDIY XATO TOPILDI VA TUZATILDI: `/auth/logout` va `/auth/refresh` tanasiz so'rovni `400` bilan rad etardi (`z.object()` `undefined` ni qabul qilmaydi), ya'ni BRAUZERDAN CHIQISH UMUMAN ISHLAMASDI — foydalanuvchi "chiqdim" deb o'ylab, sessiyasi ochiq qolardi va token yangilash ham hech qachon bajarilmasdi; mavjud testlar har doim tanada token yuborgani uchun buni ko'rmagan, endi brauzer yo'li alohida qoplangan; qurilmagan bo'lim menyuda KO'RINADI lekin HAVOLA emas (osilgan prefetch va 404 dan qochish); admin `X-Robots-Tag: noindex` va `X-Frame-Options: DENY` sarlavhalarini beradi, CI da tekshiriladi; Dockerfile va CI qadamlari qo'shildi
S19 | 2026-09-23 | admin cms screens | branch step/S19-admin-cms | notes: 13 ta ekran IKKITA umumiy komponentdan quriladi (`ResourceScreen` — CRUD, `UpsertScreen` — kalit bo'yicha saqlash); alohida yozilsa ular vaqt o'tib bir-biridan farq qilib ketardi (biri xato holatini ko'rsatardi, boshqasi o'chirishni so'ramasdan o'chirardi); ko'p tilli muharrir uz/ru/en ichki ko'rinishlarda va to'ldirilgan til nuqta bilan belgilanadi; media tanlagich ichida yuklash ham bor; JIDDIY O'ZGARISH: `localizedSchema` endi KAMIDA BITTA tilni talab qiladi, uchalasini emas — eski qoida "muharrir o'zbekcha yozadi, tarjimon keyin qo'shadi" oqimini butunlay bloklardi, va uning asosidagi izoh ("tarjimasiz mahsulot bo'sh sarlavha bilan chiqardi") S12 dagi `text()` zaxira mexanizmi tufayli endi to'g'ri emas; bo'sh satrlar tekshiruvdan OLDIN olib tashlanadi, ya'ni `{uz:'X',ru:'',en:''}` bazaga `{uz:'X'}` bo'lib tushadi; `cleanPayload()` bo'sh ixtiyoriy maydonlarni umuman yubormaydi (`null` va bo'sh ko'p tilli obyekt serverda "berilgan, lekin noto'g'ri" bo'lib tushunilardi); `apiFetch` endi `FormData` ni o'zgartirmaydi — avval `JSON.stringify(formData)` `"{}"` berib, fayl yo'qolardi; brauzerda tekshirildi: 11 ekran ochiladi, axe 0 buzilish, yaratish->nashr->tahrir->o'chirish to'liq aylanma ommaviy API ga yetib boradi (kesh bekor qilinishi bilan)
S20 | 2026-09-24 | admin leads pipeline | branch step/S20-leads-pipeline | notes: arizalar ro'yxati (server tomonda holat filtri), tafsilot sahifasi, `lead_events` dan tarix va xodimga biriktirish; o'tish tugmalari `@barff/types` dagi YAGONA jadvaldan quriladi — server ham o'shani ishlatadi, ya'ni panel ruxsat bergan o'tishni server rad etmaydi; DoD brauzerda tasdiqlandi: NEW dan `Dilerga aylandi` tugmasi umuman ko'rsatilmaydi VA API to'g'ridan-to'g'ri `fetch` bilan chaqirilganda ham 400 (`LEAD_STATUS_INVALID_TRANSITION`); har bir o'tish va biriktirish audit jurnaliga oldingi/yangi holat bilan yoziladi (testda tekshirilgan); biriktirish faqat `leads.manage` RUXSATIGA ega xodimga — rol nomi bo'yicha emas, chunki rollar o'zgaradi, ma'no ruxsatda qoladi; ariza ko'ra olmaydigan odamga biriktirilsa u javobsiz qolardi; XATO TOPILDI VA TUZATILDI: `@Get('assignees')` `@Get(':id')` dan KEYIN e'lon qilingani uchun Nest uni `:id` deb tushunib, `ParseUUIDPipe` bilan 400 qaytarardi — statik marshrut parametrli marshrutdan oldin turishi shart
S21 | 2026-09-24 | phase 1 qa and staging release | branch step/S21-phase1-qa | notes: PHASE 1 GATE — QISMAN bajarildi va buni yashirmayman: QA to'liq o'tdi, lekin STAGING'GA JOYLASH BAJARILMADI, chunki bu muhitda AWS/domen/Cloudflare kirish huquqi yo'q (Q18) — `docs/RELEASE-P1.md` da tayyor retsept va smoke test ro'yxati yozildi, "staging'da ishlayapti" deb aytilmaydi, faqat "joylashga tayyor"; uchala kritik oqim brauzerda o'tdi (ariza yuborish -> 202, admin kirishi, admin tahriri ommaviy API va saytda ko'rinishi) va sinov kontenti o'zidan keyin tozalanadi; unumdorlik 5 marshrut x 2 o'lchamda o'lchandi — LCP 76-176ms, CLS 0.0000 hammasida, dastlabki JS 207.5 KB gzip, lekin raqamlar localhost'dan va OPTIMISTIK ekani hujjatda alohida yozildi; three.js va GSAP dastlabki bundle'da YO'Q va keyin ROSTDAN ishga tushadi (1 canvas, 4 bo'lim); a11y: axe 0 buzilish 15 marshrut x 2 o'lcham, klaviatura bilan 27-36 element, ko'rinmas fokus 0, o'tkazib yuborish havolasi uchala tilda; O'LCHOV SKRIPTLARIDA UCH XATO TOPILDI VA TUZATILDI, ular ilovani noto'g'ri ayblagan edi: (1) baytlar `content-length` dan o'qilgani uchun siqilgan javoblarda ~0 chiqardi — `encodedBodySize` ga o'tkazildi, (2) "3D kechiktirilganmi" DCL dan keyingi 400ms oynasi bilan o'lchanardi, lokalda dinamik chunk ham shu oynada ulguradi — endi dastlabki HTML dagi `<script src>` teglari tekshiriladi, (3) `ScrollTrigger` va `registerPlugin(` iborasi dinamik importning O'ZIDA uchraydi — kutubxona ichki belgisi (`gsap.core`, `_gsap`, `CSSPlugin`) bo'yicha qidiriladigan bo'ldi; xuddi shunday fokus tartibi PIKSEL bo'yicha o'lchanib, ustunli futerni "orqaga sakrash" deb ayblagandi — endi fokus OLDINGI BO'LIMga qaytmasligi tekshiriladi; MUHITGA OID IKKI TUZOQ hujjatlashtirildi: `apps/admin` qayta qurilgach `next start` ham qayta ishga tushirilishi shart (eski jarayon HTML da eski chunk nomini beradi, fayl diskda yo'q -> 400 -> React gidratsiyasi butun sahifani xato ekraniga almashtiradi va forma brauzerda YO'Q bo'lib qoladi), va `POST /leads` soatiga 5 ta cheklovi takroriy sinovda 429 beradi (bu TO'G'RI xatti-harakat, sinov endi javob statusini ham ko'rsatadi); ma'lum cheklovlar ro'yxati hujjatda ochiq yozildi: galereya rasmlari lokalda ko'rinmaydi (`memory://`, production'da S3 majburiy), dinamik sahifada 404 statusi 200 qaytadi, Docker lokalda qurilmaydi; haqiqiy kontent hali yo'q — Q2, Q10, Q11, Q12, Q23, Q24 ochiq va mahsulotlar `[MOCK]` prefiksi bilan; QA skriptlari endi REPODA (`qa/`) — avval ular faqat vaqtinchalik katalogda turgan va sessiya bilan birga yo'qolardi, `qa/README.md` da ishga tushirish tartibi, ikki muhit tuzog'i va "o'lchov usuli" bo'limi (uchala yolg'on ayblov qanday yuzaga kelgani) yozildi; `qa/` ATAYLAB pnpm workspace'dan tashqarida — playwright va brauzer binarlari har bir `pnpm install` va CI uchun og'ir, bog'liqliklar faqat shu katalogda qo'lda o'rnatiladi; brauzer yo'li `CHROMIUM_PATH` dan olinadi (oldin muhitga qattiq bog'langandi); axe to'plami `memory://` xatosini endi LOKAL CHEKLOV deb belgilaydi va qizil qilmaydi — aks holda to'plam doimiy qizil bo'lib, haqiqiy xatolar shovqin ichida yo'qolardi; ildiz ESLint'ga `qa/**` uchun Node+brauzer global'lari qo'shildi (`page.evaluate()` ichidagi kod BRAUZERda bajariladi), yangi bog'liqlik qo'shilmadi — `globals` allaqachon bor edi; CI da `leads.e2e-spec.ts` dagi bitta test yiqildi (lokalda o'tardi, chunki turbo keshdan qaytargan edi) — "biriktirish AUDIT qilinadi" `/assignees` ro'yxatining BIRINCHI yozuviga tayanardi, ro'yxat esa BUTUN bazadagi `leads.manage` egalari va vitest test FAYLLARINI parallel yurgizadi, ya'ni `[0]` boshqa fayl yaratgan va istalgan payt o'chiradigan foydalanuvchi bo'lishi mumkin edi; endi test faqat O'ZI yaratgan admin'ga tayanadi va ro'yxatda o'sha admin borligini nomma-nom talab qiladi
S21A | 2026-09-24 | light theme | branch step/S21A-light-theme | notes: REJADAN TASHQARI, buyurtmachi so'rovi bilan — sayt faqat qorong'i edi va `CLAUDE.md` §16 ham shuni aytardi, shuning uchun hujjat ham yangilandi (§16 ga "Light and dark" bo'limi qo'shildi); har bir rang BIR MARTA yoziladi va ikkala qiymatni `light-dark()` ichida saqlaydi — ikkita alohida blokda har bir token ikki joyda turardi va vaqt o'tib biri yangilanib, ikkinchisi eskirib qolardi; qaysi qiymat ishlashini `color-scheme` hal qiladi va u brauzerning O'Z elementlarini (skrollbar, forma boshqaruvlari) ham moslaydi; XOM palitra (`brand-400/500`) endi komponentlarda TO'G'RIDAN-TO'G'RI ISHLATILMAYDI — bitta xom rang ikkala fonda ishlay olmaydi: `brand-400` qora fonda 9.79:1, oq fonda esa 1.96:1 beradi; o'rniga MA'NOVIY tokenlar: `accent` (yuza), `accent-hover`, `accent-on` (yuzadagi matn — ikkala ko'rinishda ham QORA, chunki oq matn yashilda 2.72:1), `accent-text` (sahifa fonidagi aksent matn), `focus`, `danger-on`; fon shkalasi (`ink-900..600`) ma'nosi aniqlashtirildi — raqam KO'Z DAN UZOQLIK, rang emas, shuning uchun u ko'rinishlar orasida teskari aylanadi; almashtirgich UCH holatli (`system`/`light`/`dark`), ikkita emas — qat'iy tanlov kunduzi yorug', kechqurun qorong'i bo'ladigan tizimda tanlovni bekor qilib qo'yardi; `system` da atribut butunlay OLIB TASHLANADI, chunki CSS tizim afzalligini `:root:not([data-theme])` orqali ushlaydi; tanlov chizishdan OLDIN, `<head>` dagi sinxron skript bilan qo'llanadi — aks holda sayt statik qurilgani uchun yorug' rejimdagi foydalanuvchi har safar bir lahza qorong'i ekranni ko'rardi; almashtirgich ommaviy sarlavhada, mobil menyu ichida (360px da sarlavhaga sig'maydi) va admin panelda; IKKI HAQIQIY KAMCHILIK MAVJUD QORONG'I KO'RINISHDA TOPILDI va tuzatildi — test ikkala ko'rinish va BARCHA fon qatlamlari uchun kengaytirilganda chiqdi: (1) `fg-subtle` eng ochiq sirtda (`ink-600`) 4.30:1 ga tushardi, eski test bu qatlamni umuman tekshirmagan chunki u paytda token ishlatilmasdi — #7a827f dan #878f8c ga, (2) `danger` kartochka fonida (`ink-700`) 4.43:1 berardi, ya'ni kartochka ichidagi xato xabari AA dan o'tmasdi, sahifa fonida 4.86:1 bergani uchun eski test ko'rmagan — #d94b3f dan #e4665a ga; kontrast testi endi 42 ta tekshiruv, har biri ikkala ko'rinishda, va hover holati ham alohida (hover FONNI o'zgartiradi, matn esa o'sha-o'sha qoladi); brauzerda tekshirildi: axe 15 marshrut x 2 o'lcham x 2 ko'rinish = 0 buzilish, `qa/theme.mjs` da almashtirish aylanishi, xotiraga yozilishi, FOUC yo'qligi (`waitUntil:'commit'` nuqtasida atribut allaqachon turadi) va `localStorage` BLOKLANGAN brauzerda sayt xatosiz ishlashi hamda almashtirish baribir ishlashi
S21B | 2026-09-24 | august visual direction, motion, brand and cPanel | branch step/S21B-august-visual | notes: REJADAN TASHQARI, buyurtmachi so'rovi bilan — u 2026-08-26 dagi qurilishni (git `a86f349`) ko'rsatib "aynan shu dizayn yoqqandi" dedi; o'sha qurilish o'z akkauntidan o'chirilgan ekan, git tarixidan topildi va tiklandi; YO'NALISH ALMASHDI: yorug', nashriy (editorial) interfeys endi ASOSIY, qorong'i esa ikkinchi darajali, lekin TENG HUQUQLI variant — ikkalasi ham bir xil tekshiruvlardan o'tadi (`CLAUDE.md` §16 va §17 qayta yozildi); PALITRA ATAYLAB NEYTRAL: qora/oq/kulrang, rang MAHSULOT RASMIDAN keladi — butun saytni o'ylab topilgan yashilga bo'yash taxmin bo'lardi, keyinchalik logotipdan O'LCHANGAN yashil faqat AKSENT tokenlariga qo'yildi (Q2 hali qisman: SVG kutilmoqda); tipografika token'lari (`--text-hero/section/title/lead/label`) va ularning `line-height`/`letter-spacing` juftlari YAGONA manbada, `next/font` (Manrope + Inter, `cyrillic` bilan) endi ROSTDAN ulangan — avval umuman ulanmagan ekan; HARAKAT: `motion/config.ts` doimiylar, `motion/recipes.ts` retseptlar (GSAP'ni STATIK import qiladi va shuning uchun FAQAT `await import()` orqali chaqiriladi — aks holda GSAP dastlabki bundle'ga tushardi), Lenis va ScrollTrigger BITTA `gsap.ticker` rAF bilan sinxron (`lagSmoothing(0)`) — ikkita mustaqil sikl skrollni ikki marta hisoblardi; WEBGL ENDI MAHSULOTNI CHIZMAYDI: eski 3D hero kod ichida qurilgan shisha edi, ya'ni O'YLAB TOPILGAN shakl, va u ekranda tiklangan tasvir bilan yonma-yon ikkita boshqa shisha berardi — Three.js `HeroField.tsx` da ambient zarracha maydoni sifatida qoldi (`three` dinamik, faqat harakat yoqilgan + WebGL bor + keng ekranda); HAQIQIY KAMCHILIKLAR TOPILDI VA TUZATILDI: (1) GSAP SplitText standart `aria: 'auto'` bilan taqiqlangan `aria-label` yozib, 8 sahifada `aria-prohibited-attr` bergan — birinchi urinishim (o'z `setAttribute` imni olib tashlash) ISHLAMADI va sabab kutubxonada ekani shundan aniqlandi, `aria: 'none'` bilan yopildi, (2) qorong'i ko'rinishda `fg-subtle` 4.30:1 va `danger` 4.43:1 — tuzatildi, kontrast testi 40 ta juftlikni IKKALA ko'rinishda tekshiradi, (3) avgustdagi qurilish mahsulot sarlavhasini mahsulotning O'Z rangida berardi va 2.82:1 ga tushardi — mahsulot rangi endi FAQAT bezak (fon, chiziq, yorug'lik), matn rangi emas, va bu qaror test bilan yozib qo'yildi; HOST MUSTAQILLIGI: buyurtmachining `hostmaster.uz` cPanel hostingida Redis ham, S3 ham, Docker ham yo'q — refresh token'lar va kirish urinishlari Redis'dan PostgreSQL ga ko'chirildi (bu chetlab o'tish emas, tuzatish: ilgari Redis qayta ishga tushsa HAMMA tizimdan chiqib ketardi), media uchun fayl tizimi adapteri qo'shildi (HMAC imzo `timingSafeEqual` bilan, yo'l ko'chishiga qarshi shakl + yechilgan yo'l tekshiruvi); `scripts/package-cpanel.mjs` + `docs/DEPLOY-CPANEL.md` — to'plam ROSTDAN yig'ildi, `npm install` bilan o'rnatildi va ishga tushirildi: Next standalone serveri ko'tarildi va CSS `200`, API `production` rejimida Redis'siz/S3'siz ko'tarildi, `POST /auth/login` 200 va ikkala cookie, `GET /products` haqiqiy ma'lumot; ikkita joylash nuqsoni shu tekshiruvda topildi — (a) bo'sh muhit o'zgaruvchisi (`REDIS_URL=''`) ilovani "Invalid URL" bilan yiqitardi, chunki cPanel e'lon qilingan har bir o'zgaruvchini bo'sh satr sifatida uzatadi, endi bo'sh satr "berilmagan" deb qaraladi, (b) `NEXT_PUBLIC_*` turbo'da e'lon qilinmagani uchun sayt API manzilisiz qurilardi va barcha kontent sahifalari "ma'lumot mavjud emas" holatida chiqardi — ular `build.env` ga (kesh kalitiga) qo'shildi; ICHKI SAHIFALAR endi `display-1..4`/`eyebrow`/`lead` umumiy sinflaridan foydalanadi — avval har bir komponent token'larni o'zi yig'ardi va ichki sahifalar bosh sahifadan kichik ko'rinardi; LOGOTIP SHAFFOFLIGI BAJARILMADI va buni yashirmayman: chekkadan to'ldirish (flood fill) ko'ylak, galstuk va harf ichini teshib o'tdi (markazdagi figuraning ko'ylagi yoqa orqali tashqariga ulangan), qizil fonda kompozitsiya qilib tekshirildi — SVG so'raldi; `qa/README.md` ga yangi tuzoq: `pkill -f "next start"` serverni O'LDIRMAYDI, Next jarayon nomini `next-server` ga o'zgartiradi, eski server portni ushlab turadi va yangisi jim yiqiladi — bu shu ishda "sayt uslubsiz chiqdi" degan noto'g'ri xulosaga olib keldi, aslida eski qurilish xizmat qilayotgan edi
S22 | 2026-09-24 | dealers domain and approval flow | branch step/S22-dealers | notes: 4 model (`dealer_tiers`, `dealers`, `dealer_addresses`, `dealer_events`) va ommaviy ro'yxatdan o'tish; ARIZA IKKI NARSANI BIRGA yaratadi — kirish akkaunti va tashkilot, bitta tranzaksiyada: akkaunt yaratilib diler yozuvi yaratilmay qolsa, hech qayerga tegishli bo'lmagan "yetim" akkaunt qolardi; akkaunt DARHOL `DEALER` rolini oladi, lekin bu KIRISH HUQUQINI BERMAYDI — diler endpoint'lari `status === APPROVED` ni talab qiladi, ya'ni rolning o'zi hech narsani ochmaydi, va tasdiqlanmagan diler o'z arizasi holatini KO'RA OLADI (aks holda u qo'ng'iroq qilishga majbur bo'lardi); `dealerId` HECH QACHON so'rovdan olinmaydi, u joriy foydalanuvchi yozuvidan keladi — aks holda diler boshqa tashkilotning id sini yozib uning ma'lumotiga yetardi (IDOR), va bu 4 ta test bilan qoplangan (begona manzil ro'yxatda ko'rinmaydi, tahrirlab/o'chirib bo'lmaydi va yozuv O'ZGARMAGANI ham tekshiriladi); BAND EMAIL uchun javob BIR XIL (`{accepted:true}`, yangi yozuv yaratilmaydi) — aks holda endpoint kim qaysi email bilan ro'yxatdan o'tganini tekshirish vositasiga aylanardi, STIR esa ochiq 409 beradi chunki u ommaviy ma'lumot va takrorlanishi haqiqiy xato; o'tish jadvali `@barff/types` da va SERVER ham panel ham o'shani ishlatadi — `PENDING->SUSPENDED` YO'Q (to'xtatish faqat faol dilerga ma'noli), `REJECTED->PENDING` BOR (hujjat to'g'rilangan bo'lishi mumkin, rad etish oxirgi so'z emas), lekin `REJECTED->APPROVED` yo'q; rad etish va to'xtatish uchun SABAB majburiy — sababsiz "rad etildi" dilerni qo'ng'iroqqa majbur qiladi; `internalNote` dilerga QAYTARILMAYDI va bu test bilan qoplangan; chegirma FOIZ emas, BAZIS PUNKT (250 = 2.5%) — pul hisobida kasr son ishlatilmaydi; birinchi manzil AVTOMATIK standart bo'ladi (aks holda buyurtma oqimi yarim yo'lda to'xtardi), standart manzil o'chirilsa u qolganlarning eng eskisiga o'tadi, manzillar yumshoq o'chiriladi chunki buyurtmalar ularga havola qiladi (S26); ro'yxatdan o'tish soatiga 5 ta — bu lead formasidagidan MUHIMROQ, chunki endpoint AKKAUNT yaratadi, va chegara alohida faylda tekshiriladi; O'LCHAB TOPILGAN XATO: `setStatus` da `users.invalidate()` chaqirig'i va "usiz diler kesh muddati tugaguncha ishlayverardi" degan izoh bor edi — chaqiruvni o'chirib testni qayta yurgizdim va 22 tadan 22 tasi BARIBIR o'tdi, chunki keshda faqat rol va ruxsat turadi, diler holati esa har safar bazadan o'qiladi; chaqiruv ham, izoh ham olib tashlandi — ishlamaydigan kodni to'g'ri ishlayotgandek ko'rsatuvchi izoh keyinchalik kimnidir unga TAYANTIRARDI; CI HAQIQIY NUQSONNI KO'RSATDI va u tuzatildi: `NotificationsService` boshidagi izohda "bildirishnoma yuborilmagani ASOSIY amalni buzmaydi" deb yozilgan edi, lekin bu AMALDA BAJARILMAGAN edi — `sendInApp` ichidagi baza xatosi `notify()` dan yuqoriga chiqib, diler arizasini `202` o'rniga `500` qilardi, garchi diler bazaga YOZILGAN bo'lsa ham; CI'da sabab aniq ko'rindi (qabul qiluvchilar ro'yxati O'QILGANDAN keyin, yozuv kiritilishidan OLDIN o'sha admin parallel test tomonidan o'chirilgan va tashqi kalit buzilgan), lekin bu faqat test masalasi emas: production'da ham admin o'chirilgan lahzada kelgan ariza YO'QOLARDI; endi ikkala yo'nalish alohida o'raladi (biri yiqilsa ikkinchisi baribir ishlaydi) va xato logga yoziladi — 4 ta test, ularning 3 tasi eski xatti-harakat bilan yiqilishi tekshirildi; 23 ta e2e test, 16 ta o'tish testi; `exactOptionalPropertyTypes` bilan `Partial<T>` DTO uchun yaramaydi (u "maydon yo'q" degani, DTO esa "maydon bor va undefined" beradi) — mapped tip qo'shildi
S23 | 2026-09-24 | dealer pricing engine | branch step/S23-pricing | notes: `price_rules` — BITTA jadval, har turga alohida EMAS: daraja narxi, hududiy narx, hajm chegirmasi va aksiya bir xil savolga javob beradi ("shu dilerga, shu variant uchun, shu miqdorda qancha?"), alohida jadvallarda esa ularning USTUNLIGI kod bo'ylab tarqalib ketardi va "qaysi narx qo'llandi" degan savolga javob berib bo'lmasdi; HISOB SOF FUNKSIYADA (`price-resolver.ts`) — baza ham, Nest ham yo'q, shuning uchun har bir ustunlik holati arzon va aniq sinaladi, ma'lumot yuklash esa alohida (`pricing.service.ts`); IKKI BOSQICH va har bosqichda FAQAT BITTA qoida — 1) narx: eng ustun `FIXED_PRICE`, yo'q bo'lsa bazaviy, 2) chegirma: eng ustun chegirma qoidasi, yo'q bo'lsa DARAJA chegirmasi; chegirmalar USTMA-UST QO'YILMAYDI, chunki uchta 10% lik qoida 30% emas, 27.1% beradi va buni mijozga tushuntirib bo'lmaydi; USTUNLIK tartibi to'liq va qat'iy: KIMGA (diler > daraja > hudud) > NIMAGA (variant > mahsulot > kategoriya) > `minQuantity` > `priority` > `validFrom` > `id` — "kimga" "nimaga" dan ustun, chunki narx MIJOZ bilan kelishiladi; oxirgi `id` solishtiruvi bo'lmasa natija bazadagi TARTIBGA bog'liq bo'lib qolardi (`Array.sort` barqaror) va bir xil so'rov turli paytda turli narx berishi mumkin edi; AKSIYA KODI faqat kod kiritilganda hisobga olinadi (aks holda har kim uni bilmasdan olardi) va odatdagi g'olib bilan solishtirilib MIJOZ FOYDASIGA hal qilinadi — aks holda kod kiritgan mijoz undan ZARAR ko'rishi mumkin edi; pul butun tiyinda, foiz bazis punktda (250 = 2.5%), chegirma hech qachon narxdan oshmaydi; TESTLAR MUTATSIYA BILAN TEKSHIRILDI — 34 ta test birinchi urinishda o'tgani shubha tug'dirdi, shuning uchun resolverga ataylab 6 ta xato kiritib ko'rdim: 5 tasi ushlandi, LEKIN `id` bo'yicha oxirgi solishtiruvni olib tashlaganimda 34 tadan 34 tasi BARIBIR o'tdi, ya'ni aniqlik qoidasi himoyalanmagan edi — butunlay teng ikki qoida uchun test qo'shildi va mutatsiya endi ushlanadi (35 ta test); admin ekrani `ResourceScreen` ustida va BRAUZERDA haydab tekshirildi: qoida yaratildi, ro'yxatda "15.00%" va "hammaga · butun katalog" bo'lib chiqdi, konsol xatosi yo'q; `amount` ning MA'NOSI `kind` ga bog'liq bo'lgani uchun forma yorlig'i ham, jadval ustuni ham dinamik — xodim 20% o'rniga 2000% yozib qo'ymasligi uchun; controller'da `dto as unknown as Prisma...` ikki bosqichli o'tkazish bor edi va u HAQIQIY nomuvofiqlikni yashirardi — o'z kiritma tipi (`PriceRuleWrite`) va aniq o'girish bilan almashtirildi; 17 e2e test; MUHIT TUZOG'I takrorlandi va hujjatlashtirildi: `pkill` boshqa buyruq bilan birga yozilib, o'sha buyruq yiqilgani uchun UMUMAN ishlamadi — eski API port 3000 ni ushlab turdi va admin panel `404` oldi, ekran "buzuq" ko'rindi; ishonchli usul jarayonning YOSHINI ko'rish (`ps -o lstart`) va marshrut bilan tasdiqlash (`404` — eski build, `401` — yangi)
S24 | 2026-09-24 | dealer portal shell | branch step/S24-dealer-shell | notes: `apps/dealer` (3003-port, `partner.barff.uz`) — kirish, himoyalangan layout, navigatsiya, boshqaruv paneli, manzillar va profil; ADMIN PANELIDAN ASOSIY FARQI: bu yerda ROL yetarli emas, DILER HOLATI hal qiladi — `DEALER` roli ariza yuborilgan zahoti beriladi (S22), ya'ni u hech narsani ochmaydi, shuning uchun sessiya `/auth/me` bilan birga `/dealer/profile` ni ham o'qiydi va menyu `status === APPROVED` ga qarab quriladi; TASDIQLANMAGAN diler menyuda savdo bo'limlarini UMUMAN ko'rmaydi va `/addresses` ga to'g'ridan-to'g'ri kirsa `/` ga yo'naltiriladi — bu QULAYLIK qatlami, haqiqiy himoya serverda (`requireActiveDealer` 403 qaytaradi) va ikkalasi ham brauzerda tekshirildi; boshqaruv paneli IKKI XIL ko'rinadi — tasdiqlanmagan dilerga faqat ariza holati (bo'sh "0 ta buyurtma" kartochkalari uni "nimadir buzilgan" deb o'ylashga majbur qilardi), tasdiqlanganiga savdo ko'rsatkichlari; buyurtma va balans uchun NOL ko'rsatilmaydi, "tez orada" yoziladi — ma'lumot hali YO'Q, nol esa haqiqat emas (CLAUDE.md §1); MOBIL BIRINCHI, chunki dilerlar TELEFONDAN buyurtma beradi: yon menyu faqat `lg` dan boshlab, kichik ekranda u gorizontal lentaga aylanadi (burger EMAS — diler katalog/savat/buyurtmalar orasida tez-tez yuradi va har safar menyu ochish ikki bosish qo'shardi), tugmalar eng kam 44px; manzillar to'liq CRUD va u MIJOZDA o'qiladi — telefonda har qayta yuklash sekin; bo'sh ixtiyoriy maydonlar YUBORILMAYDI (server `district: ''` ni "berilgan, lekin noto'g'ri" deb tushunardi); standart manzilni "olib tashlash" yo'q, faqat BOSHQASIGA ko'chirish — serverdagi qoida bilan bir xil (standartsiz diler buyurtma bera olmasdi); kirish sahifasida "hali hamkor emasmisiz?" havolasi ommaviy saytdagi ariza formasiga olib boradi — aks holda diler bo'lmagan odam "parol noto'g'ri" bilan qolardi; portal `X-Robots-Tag: noindex` beradi va buni CI Docker tasvirida tekshiradi — bu yerda DILER NARXLARI bor, ular ommaviy emas; BRAUZERDA TEKSHIRILDI 390px va 1440px da, tasdiqlanmagan VA tasdiqlangan holatda: ariza holati ko'rinadi, savat menyuda yo'q, `/addresses` yo'naltiriladi, tasdiqlangach menyu ochiladi, manzil qo'shildi va birinchi manzil avtomatik standart bo'ldi, gorizontal skroll 0, konsol xatosi yo'q; cPanel to'plami va qo'llanmasi ham yangilandi (endi to'rtta Node ilovasi)
S25 | 2026-09-24 | dealer catalog and cart | branch step/S25-catalog-cart | notes: `GET /dealer/products` (diler narxi bilan, kategoriya/hajm/qidiruv filtrlari) va SERVERDA saqlanadigan savat (`carts`, `cart_items`); IKKI QAT'IY QOIDA: (1) NARX SAVATDA SAQLANMAYDI — `cart_items` da faqat variant va miqdor bor, narx har o'qishda `PricingService` orqali qayta hisoblanadi, aks holda bir hafta ochiq turgan savat eski narxni ushlab qolardi va bu test bilan qoplangan (qoida qo'shilgach savat jami DARHOL o'zgaradi), (2) MIJOZDAN KELGAN NARX UMUMAN O'QILMAYDI — sxemada narx maydoni YO'Q, test `unitPrice/price/total/discount` ni birga yuboradi va narx o'zgarmasligini tekshiradi (S25 DoD); savat QURILMA ALMASHGANDA saqlanadi — bu ham test bilan qoplangan, LEKIN birinchi yozgan testim NOTO'G'RI edi: u ikki kirishning access token'lari FARQ QILISHINI talab qilardi va yiqildi, chunki JWT `iat` butun soniyada — bir soniya ichidagi ikki kirish BAYT-BAYT bir xil token beradi; sessiya alohida ekanini refresh cookie ko'rsatadi va test shunga o'tkazildi; `dealerId` va savat id SO'ROVDA YO'Q — ikkalasi ham sessiyadan, begona diler bizning pozitsiyamizga `404` oladi va yozuv o'zgarmagani ham tekshiriladi; bir xil variant savatda ikki marta turmaydi (miqdor qo'shiladi); eng kam buyurtma miqdori buzilsa BELGILANADI, lekin QO'SHISH rad etilmaydi — diler miqdorni bosqichma-bosqich oshiradi va har bosishda xato ko'rish chalg'itardi, tekshiruv buyurtma yuborishda majburiy bo'ladi (S26); noma'lum aksiya kodi XATO BERMAYDI — "kod noto'g'ri" javobi qaysi kod BOR ekanini taxmin qilish vositasiga aylanardi; UI: mobil birinchi, miqdor tugmalari 44px va `inputMode="numeric"`, har amal serverdan yangilangan savatni oladi va keshga yozadi (mijozda "miqdor × narx" ham hisoblanmaydi — hajm chegirmasi miqdorga bog'liq); PUL FORMATI MUHITGA BOG'LIQ EKANI O'LCHAB TOPILDI VA TUZATILDI: `Intl` ning `style: 'currency'` i UZS uchun Node'da `900 soʻm`, Chromium'da `UZS 900` beradi, oddiy `NumberFormat` ham Node'da `18 000`, Chromium'da `18,000` — ya'ni serverda chizilgan narx brauzerda qayta chizilganda O'ZGARIB ko'rinardi (React gidratsiya nomuvofiqligi, mijoz uchun esa "narx boshqacha"); `formatMoney` endi `Intl` ni UMUMAN ishlatmaydi, guruhlash tor ajralmas bo'shliq bilan QO'LDA qilinadi, yorliq ham qo'lda, va u `@barff/utils` dagi YAGONA funksiya — web ham, diler portali ham o'shani ishlatadi (avval web o'z nusxasiga ega edi); 8 ta test qulflaydi va brauzerda tasdiqlandi: server HTML va brauzer BIR XIL (`18 000 so'm`), gidratsiya ogohlantirishi yo'q; 20 e2e test; LOKAL TUZOQ: qo'lda sinov uchun yaratgan QAMROVSIZ narx qoidam butun `pricing.e2e-spec.ts` ni "expected 85000 to be 100000" bilan yiqitdi (u BARCHA mahsulotga tegishli) — endi to'plam boshida tekshiruv bor va sababni O'ZI aytadi
S26 | 2026-09-24 | orders domain and state machine | branch step/S26-orders | notes: `orders`, `order_items`, `order_status_history` va `order_counters`; NARX YUBORISH PAYTIDA MUZLATILADI — savatda u har safar qayta hisoblanadi (S25), buyurtmada esa yo'q, chunki mijoz ko'rgan summa bilan hisob-faktura FARQ QILMASLIGI kerak; test buni ko'rsatadi: buyurtmadan keyin 50% chegirma qoidasi qo'shiladi va buyurtma summasi O'ZGARMAYDI; mahsulot nomi va MANZIL ham NUSXA sifatida saqlanadi — diler manzilni keyin o'chirsa eski buyurtma QAYERGA yetkazilgani yo'qolardi, havola ham qoladi lekin matn buyurtmaning O'ZIDA; BUYURTMA RAQAMI (`BRF-2026-000123`) alohida hisoblagich jadvalidan `INSERT ... ON CONFLICT DO UPDATE RETURNING` bilan olinadi — `COUNT(*) + 1` poygada ikki bir xil raqam berardi va `number` yagona bo'lgani uchun biri yiqilardi; raqam YIL bo'yicha qayta boshlanadi; eng kam buyurtma miqdori YUBORISHDA majburiy (savatga qo'shishda faqat belgilanadi — diler miqdorni bosqichma-bosqich oshiradi va har bosishda xato ko'rish chalg'itardi); ruxsat etilmagan o'tish `409` beradi, `400` emas: so'rov TO'G'RI, lekin resursning JORIY HOLATI unga yo'l qo'ymaydi; har bir o'tish `order_status_history` ga OLDINGI holat bilan birga yoziladi va audit jurnaliga ham tushadi; diler faqat KO'RIB CHIQILMAGAN buyurtmani bekor qila oladi — tasdiqlangandan keyin ombor ishga tushadi (S30) va bu xodim qarori; O'LCHAB TOPILGAN NUQSON — POYGA: takroriy kalit tekshiruvi KETMA-KET yuborishni ushlaydi, lekin BIR VAQTDA kelgan ikki so'rov ikkalasi ham "yo'q" deb ko'radi va ikkalasi ham yaratmoqchi bo'ladi; bazadagi yagona indeks bittasini to'xtatadi, LEKIN birinchi versiyada u xato tashlab `500` bo'lardi va men buni test logida ko'rdim — buyurtma AYNI PAYTDA yaratilgan bo'lsa ham diler "Internal Server Error" ko'rardi, ya'ni aynan shu uni QAYTA yuborishga undardi; endi P2002 ushlanadi va mavjud buyurtma qaytariladi, test esa "kamida bittasi o'tdi" bilan kifoyalanmay IKKALASI ham `201` va BIR XIL buyurtma ekanini talab qiladi (mutatsiya bilan tekshirildi: ushlagich olib tashlansa test yiqiladi); 19 e2e test, jami 382 API testi
S27 | 2026-09-24 | dealer order flow ui | branch step/S27-order-ui | notes: rasmiylashtirish (manzil -> ko'rib chiqish -> yuborish -> tasdiq), buyurtmalar ro'yxati, tafsilot sahifasi holat VAQT CHIZIG'I bilan va takroriy buyurtma; oqim SAVAT SAHIFASINING O'ZIDA qoladi — alohida sahifaga o'tkazish telefonda ortiqcha yuklash va "orqaga" bosilganda savat yo'qolishi xavfini qo'shardi; IDEMPOTENTLIK KALITI komponent BIRINCHI CHIZILGANDA bir marta yaratiladi (`useMemo` bo'sh bog'liqlik bilan), yuborish tugmasi bosilganda EMAS — aks holda ikki bosish ikki xil kalit berardi va S26 dagi himoya UMUMAN ishlamasdi; HAR BIR QADAMDA XATODAN CHIQISH YO'LI BOR (S27 DoD): manzil yo'q bo'lsa "Manzil qo'shish" havolasi, ko'rib chiqishda "Orqaga" (manzil noto'g'ri tanlansa savatni qaytadan yig'ishga majbur qilmaydi), yuborish xato bersa xabar va qayta urinish — kalit o'sha-o'shaligicha qolgani uchun takror buyurtma yaratilmaydi; holat matnlari bitta joyda (`order-labels.ts`) — texnik nom (`READY_FOR_DELIVERY`) dilerga ko'rsatilmaydi; vaqt chizig'i `order_status_history` dan, ya'ni "buyurtmam qayerda?" savoliga javob dilerning O'ZIDA va u qo'ng'iroq qilishga majbur emas; takroriy buyurtmada narx QAYTA hisoblanadi va bu ochiq yozilgan — eski narx tiklanmaydi, chunki u o'sha lahzaning narxi edi; ro'yxat kartochka ko'rinishida, jadval emas: telefonda jadval gorizontal skrollga majbur qiladi; BRAUZERDA 360px DA TO'LIQ OQIM HAYDALDI (S27 DoD) — manzilsiz holat yo'l ko'rsatdi, manzil qo'shildi, ko'rib chiqish ochildi, buyurtma yuborildi (`BRF-2026-000041`), tasdiq xabari va vaqt chizig'i chiqdi, savat bo'shadi, ro'yxatda ko'rindi, takroriy buyurtma savatga qo'shildi; har qadamda gorizontal skroll 0, konsol va tarmoq xatosi yo'q
S28 | 2026-09-24 | admin order management | branch step/S28-admin-orders | notes: buyurtmalar ro'yxati filtrlar bilan (holat, diler, hudud, sana oralig'i, raqam bo'yicha qidiruv), tafsilot sahifasi (pozitsiyalar, diler, tarix), ichki izoh va holat tugmalari; S26 DA RUXSAT XATOSI BOR EDI va u shu qadamda topildi: holat o'zgartirish `orders.manage` bilan qo'riqlangan edi, lekin ruxsat katalogida OMBOR xodimiga `orders.status.change` berilgan — ya'ni ombor xodimi buyurtmani "PICKING" ga o'tkaza OLMASDI va butun Faza 3 oqimi bloklanardi; endpoint endi katalogdagi ruxsatga mos va test mutatsiya bilan tekshirildi (eski ruxsat qaytarilsa 4 ta test yiqiladi); bildirishnoma manzili ham to'g'rilandi — `orders.manage` faqat adminda edi, ya'ni yangi buyurtmadan sotuvchi ham, ombor ham XABARDOR BO'LMASDI, endi `orders.view`; ICHKI IZOH qo'shildi (`orders.internalNote`) va u dilerga QAYTARILMAYDI — `ORDER_SELECT` da bu maydon ataylab yo'q, test buni ikki tomondan tekshiradi (diler ko'rmaydi, admin ko'radi); SANA FILTRI KUN OXIRIGACHA: `createdAt <= to` bilan `to` = bugun berilsa bugungi buyurtmalar TUSHIB QOLARDI va xodim "bugungi buyurtmalar" ni so'rab bo'sh ro'yxat ko'rardi; panel holat tugmalarini `@barff/types` dagi YAGONA o'tish jadvalidan quradi, ya'ni panel ko'rsatgan tugmani server rad etmaydi; 19 e2e test rol qamrovini tekshiradi (SALES ko'radi lekin o'zgartira olmaydi, WAREHOUSE o'zgartiradi lekin ichki izoh yoza olmaydi, DRIVER umuman ko'rmaydi, diler 403); MUHIT TUZOG'I — YANGI TURI: admin panel oq ekranga aylandi ("Cannot read properties of undefined") va men uni KOD xatosi deb o'yladim, aslida server eski javob shaklini qaytarayotgan edi; "`401` qaytdi, demak marshrut bor, build yangi" degan xulosam NOTO'G'RI edi — `:id` marshruti `dealers` so'zini ham qabul qiladi va autentifikatsiya validatsiyadan OLDIN ishlaydi, ya'ni ESKI build ham `401` beradi; `qa/README.md` ga ishonchli tekshiruv yozildi: statusga emas, JAVOBNING O'ZIGA qarash kerak; shu munosabat bilan admin tafsilot sahifasi ham mustahkamlandi — har bir maydon ixtiyoriy zanjir bilan o'qiladi va bo'sh maydon uchun "—" ko'rsatiladi, chunki bitta maydon yetishmagani uchun xodim HECH NARSA ko'rmasligi qabul qilinmaydi
S29 | 2026-09-24 | phase 2 qa release | branch step/S29-phase2-qa | notes: FAZA 2 DARVOZASI — "diler qo'lda aralashuvsiz buyurtma bera oladi" sharti brauzerda tekshirildi va O'TDI (18/18, konsolda 0 xato); DARVOZA S28 TUGAGANDA HAM BAJARILMASDI, chunki zanjirning IKKITA bo'g'ini umuman yo'q edi — diler arizasi formasi (`POST /dealers/register` S22 da qurilgan, lekin unga hech qanday ekran ulanmagan) va admin paneldagi dilerlar ekrani (`/dealers` menyuda `ready: false` turardi, ya'ni yangi dilerni tasdiqlashning yagona yo'li API'ni qo'lda chaqirish edi); ikkalasi shu qadamda qo'shildi, ularsiz "qo'lda aralashuvsiz" degan shart YOLG'ON bo'lardi; HAQIQIY NOSOZLIK TOPILDI: `POST /dealers/register` band telefon uchun `500` qaytarardi — `User.phone` `@unique`, lekin email va STIR tekshirilib telefon tushib qolgan edi, ya'ni bitta ofisdan ikkinchi odam ariza yuborsa "server xatosi" ko'rardi; buni kodni o'qib emas, sinovni IKKINCHI MARTA yurgizib topdim (birinchi yurish yashil edi); tuzatish emaildagi bilan bir xil — `202` va `{accepted: true}`, lekin yozuv yaratilmaydi (aks holda javob kimning raqami ro'yxatda borligini oshkor qilardi), poyga uchun `P2002` alohida qo'lga olinadi chunki tekshiruv bilan `INSERT` orasida oyna bor; mutatsiya bilan tasdiqlandi — tuzatish olib tashlansa test aynan `500` bilan yiqiladi; YUK SOG'LIG'I: katalog 200 so'rov / 20 parallel — p50 73ms, p95 116ms, 253 so'rov/s, 0 xato; 3 diler BIR VAQTDA buyurtma berdi va 3 ta NOYOB raqam oldi (S26 dagi atomar hisoblagich da'vosi shu bilan sinaladi — bitta diler bilan buni sinab BO'LMAYDI, chunki savat bitta va buyurtmadan keyin bo'shaydi); bir xil `idempotencyKey` bilan PARALLEL yuborilgan ikki so'rov bitta buyurtma yaratdi; SINOVNING O'ZIDA UCHTA XATO TOPILDI VA TUZATILDI: (1) telefon qattiq yozilgan edi — ikkinchi yurish JIM yiqildi va aybdor ilova emas, sinov edi, (2) `To‘xtatilgan` dagi apostrof tipografik (U+2018) lekin yorliq faylida ASCII yozilgan edi va `getByRole` tugmani topa olmadi, (3) IKKITA tekshiruv YOLG'ON YASHIL edi — "Tasdiqlangan matni ko'rindimi" tugmaning O'ZIDA ham bor edi va tasdiqlash umuman ishlamasa ham o'tardi, "savat bo'sh emas" esa savat YUKLANMAGANDA ham o'tardi; ikkalasi mutatsiya yurgizganda ko'rindi va tugmalarning ALMASHISHI hamda "Jami" qatorining BORLIGI bilan almashtirildi; `load-phase2.mjs` tezlik cheklovini sezadi va o'lchovni "ishonchsiz" deb belgilab `2` kodi bilan chiqadi — `p95` aslida CHEKLOVNING tezligini ko'rsatardi; sinov yozuvlari uchun `cleanup-phase2.mjs` (diler va buyurtmani o'chirish endpointi ATAYLAB yo'q — ikkalasi biznes yozuvi); STAGING'GA JOYLASH BAJARILMADI — muhit ma'lum (Q18: hostmaster.uz cPanel) va tartib `DEPLOY-CPANEL.md` da, lekin serverga kirish ma'lumotlari bu muhitda yo'q va bo'lmasligi ham kerak (`CLAUDE.md` §12); `docs/RELEASE-P2.md`
S30 | 2026-09-25 | warehouse stock and movements | branch step/S30-warehouse-stock | notes: QOLDIQ = HARAKATLAR JURNALINING PROYEKSIYASI, va bu BAZADA ta'minlanadi — servis intizomi bilan emas; DoD "stock can never change without a movement row" degani "hech qachon", ya'ni har qanday kod `warehouse_stock` ni to'g'ridan-to'g'ri yangilay olsa, qoldiq jurnaldan ajralib ketardi va omborchi raqamni ko'rib, uning QAYERDAN kelganini hech kim ayta olmasdi; shuning uchun uchta trigger: (1) `stock_movements` ga qator qo'shilishi qoldiqni ATOMAR o'zgartiradi (`INSERT ... ON CONFLICT DO UPDATE SET quantity = quantity + delta`), (2) `warehouse_stock` ni to'g'ridan-to'g'ri yangilash `pg_trigger_depth()` bilan RAD ETILADI, (3) jurnal qatori tahrirlanmaydi va o'chirilmaydi — xato harakat `ADJUSTMENT` bilan to'g'rilanadi; manfiy qoldiq, qoldiqdan ko'p bandlash va sababsiz tuzatish ham BAZADA rad etiladi, chunki ilovadagi tekshiruv poygada o'tkazib yuborilardi; POYGA O'LCHANDI: sakkizta parallel kelim — yo'qotish yo'q; o'nta qoldiqqa beshta parallel chiqim (har biri 4 dona) — ikkitasi o'tdi, uchtasi rad etildi, qoldiq 2, ya'ni ilovadagi "yetarlimi" tekshiruvi bilan BESHALASI ham o'tib ketardi; HAQIQIY NOSOZLIK TOPILDI (S25 dan qolgan): narxi belgilanmagan BITTA variant butun diler katalogini `400` ga aylantirardi — `quote()` `PRICE_NOT_SET` tashlardi va u butun so'rovni yiqitardi, ya'ni admin yangi variant qo'shib narxini keyinroq belgilamoqchi bo'lsa, o'sha oraliqda HAMMA diler uchun katalog ishlamay qolardi; panel buni allaqachon kutardi (`price === null` uchun "Narx belgilanmagan" deb chizadi) — ikki tomon bir-biriga ZID edi; `quote()` ga `skipUnpriced` qo'shildi va u FAQAT katalog ro'yxatida ishlatiladi, savat va buyurtma esa baribir rad etadi (noma'lum narxda sotib bo'lmaydi) — ikkala holat ham test bilan qoplangan va mutatsiya bilan tasdiqlangan; `INBOUND_MOVEMENT_TYPES` olib tashlandi — u `RELEASED` ni "kelim" deb ko'rsatib, QOLDIQ va MAVJUD QOLDIQ tushunchalarini aralashtirardi; o'rniga `PHYSICAL_MOVEMENT_TYPES` va `RESERVATION_MOVEMENT_TYPES`; `movementDelta()` bazadagi trigger bilan BIR XIL qoidani ifodalaydi va ikkalasining MOSLIGI test bilan qulflandi (mutatsiya: `OUT` ishorasi almashtirilsa test yiqiladi); trigger xatolari BARQAROR kod bilan boshlanadi (`BARFF_STOCK_...`) — Prisma ularni `PrismaClientUnknownRequestError` qilib beradi va `code` ham, `meta` ham BO'SH (o'lchab tekshirilgan), ya'ni SQLSTATE yetib kelmaydi, matnning o'zbekcha qismiga tayanish esa uni tarjima qilganda jimgina buzardi; `BARFF_STOCK_DIRECT_WRITE` ATAYLAB o'girilmaydi — u foydalanuvchi xatosi emas, KOD xatosi va `500` bo'lib jurnalga tushishi kerak; SINOVNING O'ZIDA XATO: yigirmata parallel so'rov `ECONNRESET` berardi — ilovada emas, vitest fayllarni parallel yurgizgani uchun BOSHQA to'plamlar socket'siz qolardi (`orders.e2e-spec` o'sha paytda `400` olardi); sakkiztaga tushirildi, da'vo songa emas bir vaqtdalikka bog'liq; admin ekranlari: qoldiqlar (ombor/qidiruv/kam qolganlar filtri) va harakatlar jurnali — qoldiqni TO'G'RIDAN-TO'G'RI yozadigan maydon ataylab YO'Q, faqat "Harakat", "Tuzatish" va "Chegara"; brauzerda tekshirildi: harakat yozildi va jurnalda ko'rindi, sababsiz tuzatish tugmasi o'chiq, jurnalda tahrirlash tugmasi yo'q, mobil skroll yo'q, konsol toza; seed'ga standart `MAIN` ombor qo'shildi (nomi va hududi MOCK — Q11)
S31 | 2026-09-25 | reservations picking packing | branch step/S31-reservations | notes: ZAXIRA — QOLDIQ EMAS: tovar omborda turadi (`quantity` o'zgarmaydi), lekin boshqa buyurtmaga berib bo'lmaydi (`reservedQuantity` oshadi); haddan ziyod zaxiralashni bu servis EMAS, BAZA taqiqlaydi — `reservedQuantity > quantity` S30 dagi trigger bilan rad etiladi, ya'ni ikki buyurtma bir vaqtda kelsa ikkinchisi baribir o'tmaydi; S31 DoD ("over-reservation impossible under concurrent orders") aynan shu bilan qoplangan va MUTATSIYA bilan tasdiqlangan: bazadagi tekshiruv olib tashlansa poyga sinovi yiqiladi; ZAXIRA HOLAT O'ZGARISHIDAN OLDIN olinadi — teskari tartibda buyurtma "zaxiraga olindi" deb ko'rinib, aslida hech narsa ushlab turilmagan bo'lardi va buni faqat yig'ish paytida omborchi payqardi; QISMAN BAJARISH YO'Q va bu qaror `docs/WAREHOUSE-POLICY.md` da SABABI bilan yozilgan (yarim zaxiralangan buyurtmada yig'ish varaqasi, hisob-faktura va yetkazib berish savollarining birortasiga javob yo'q, eng muhimi diler bundan xabardor emas); BITTA OMBOR — bo'lib yuborish yo'q, chunki ikki ombordan yig'ilgan buyurtma ikkita jo'natma degani va yetkazib berish domeni (S32) buyurtmaga bitta yetkazmani bog'laydi; TOVAR `PACKED` DA CHIQADI va u yerda TARTIB QAT'IY: avval `RELEASED`, keyin `OUT` — teskari tartibda oraliq holat `reservedQuantity > quantity` bo'lib qolardi va baza BUTUN tranzaksiyani rad etardi, bu test bilan qulflangan; BEKOR QILISH ikki xil: `ACTIVE` zaxira `RELEASED` bo'ladi (tovar javonda edi), `FULFILLED` esa `RETURN` (tovar chiqib ketgan edi — `RELEASED` bilan qaytarib bo'lmaydi va yozilmasa qoldiq haqiqatdan kam ko'rinardi); MUDDAT hozircha QO'LDA bo'shatiladi va bu yashirilmadi: rejalashtirgich S41 da, "muddat bor" deb yozib uni ishlatmaydigan tizim qoldiqni sekin-asta yeb qo'yadi; kam qoldiq bildirishnomasi (`stock.low`) chegara BELGILANGAN qatorlar uchun — umumiy standart chegara o'ylab topilgan raqam bo'lardi (1 litrli suv va 5 litrli balon uchun u bir xil bo'la olmaydi); XATO JAVOBI SHAKLI: `details` ataylab faqat `Record<string, string[]>` bo'la oladi (xato filtri boshqa shaklni tashlaydi) — men u yerga massiv obyekt yuborgan edim va u JIMGINA yo'qoldi, sinov "undefined" ko'rdi; endi yetishmovchilik o'qiladigan matnga aylantiriladi; SINOVNING O'ZIDA XATO: `beforeEach` qoldiqni tiklardi, lekin ZAXIRALARNI bo'shatmasdi — 100 donadan 5 ga tushirish oldingi sinovdan qolgan 10 dona band bilan `reservedQuantity > quantity` beradi va baza uni rad etadi, ya'ni sinov "400" ko'rsatdi va aybdor ilova emas, tozalash TARTIBI edi; S31 dan keyin `CONFIRMED -> RESERVED` HAQIQIY qoldiq talab qiladi, shuning uchun `orders` va `admin-orders` to'plamlariga ombor qoldig'i qo'shildi (ular ilgari ombor umuman yo'q paytda yozilgan edi); admin ekranlari: yig'ish navbati (eng eski birinchi — summa bo'yicha saralash kichik dilerni cheksiz kuttirardi) va yig'ish varaqasi (belgilash MIJOZDA qoladi va saqlanmaydi, chunki bu omborchining o'zi uchun belgi, biznes yozuvi emas); brauzerda RAQAM bilan tekshirildi: qadoqlashdan keyin qoldiq 620 -> 614, band 6 -> 0; testlar 442 ta
S32 | 2026-09-25 | delivery domain | branch step/S32-delivery | notes: YETKAZMA BUYURTMANI YETAKLAYDI, aksincha emas — ikki tomonlama moslashtirish HALQA hosil qilardi (buyurtma yetkazmani yangilaydi -> u buyurtmani yangilaydi -> ...) va uni to'xtatish uchun "bu o'zgarish qayerdan keldi" degan bayroq kerak bo'lardi, u esa har bir yangi kodda unutilardi; moslik jadvali `docs/DELIVERY-POLICY.md` §2 da va koddagi `ORDER_STATUS_FOR` bilan bir xil; UCHTA yetkazma holati (`PICKED_UP`, `IN_TRANSIT`, `ARRIVED`) buyurtmaning BITTA holatiga (`IN_TRANSIT`) tushadi — dilerga "haydovchi tovarni oldi" va "haydovchi yetib keldi" farqi kerak emas, unga "yo'lda" yetarli, to'liq tarix esa `delivery_events` da qoladi; O'ZINI-O'ZIGA O'TISH O'TKAZIB YUBORILADI va bu SHART: buyurtma allaqachon `IN_TRANSIT` bo'lsa, unga qayta o'tkazish buyurtma mashinasi tomonidan rad etilardi (S26) va haydovchi "yetib keldim" tugmasini BOSA OLMASDI; `FAILED` va `CANCELLED` buyurtmaga TEGMAYDI — yetkazilmagan buyurtma bilan nima qilish (qayta urinish, bekor qilish, manzilni o'zgartirish) ODAM qarori va avtomatik "bekor qilish" eng yomoni bo'lardi, chunki tovar ombordan chiqib ketgan (S31) va uni qaytarish alohida harakat; HAYDOVCHI FAQAT O'Z YETKAZMASIGA TEGADI va boshqasini `404` ko'radi, `403` EMAS — `403` "bunday yetkazma BOR, lekin sizga emas" degani va shu bilan id larni sinab ko'rib qaysi yetkazmalar borligini aniqlash mumkin bo'lardi (diler buyurtmalari bilan bir xil yondashuv); `driverId` SO'ROVDAN OLINMAYDI, tokendan topiladi (IDOR); haydovchi O'ZIGA ish biriktira olmaydi va ishni bekor qila olmaydi — bu logist vakolati (`DRIVER_ALLOWED` ro'yxati); JONLI GPS YO'Q va koordinata maydonlari SXEMAGA ham qo'shilmagan: maydon bo'lsa uni to'ldirish talabi o'z-o'zidan paydo bo'ladi va keyin uni olib tashlash "funksiyani yo'qotish" bo'lib ko'rinadi (CLAUDE.md §6); YETKAZMA `PACKED -> READY_FOR_DELIVERY` da AVTOMATIK tug'iladi va manzil NUSXA olinadi (diler manzilini keyin o'chirsa, tovar qayerga ketgani yo'qolmasligi kerak); yetkazma yaratilishi buyurtma o'tishini YIQITMAYDI (zaxiradan farqli tartib va bu ataylab: zaxira olinmasa buyurtma o'tmasligi kerak — tovar yo'q; yetkazma esa to'sqinlik qilmasligi kerak — `READY_FOR_DELIVERY` buyurtma yetkazmasiz ham to'g'ri holat, faqat uni hech kim ko'rmaydi); HAYDOVCHI PROFILI KIRISH HUQUQI BERMAYDI — `DRIVER` roli tekshiriladi va yo'q bo'lsa ochiq aytiladi, aks holda "profil bor, lekin haydovchi tizimga kira olmaydi" degan jim holat paydo bo'lardi; `sequence_counters` jadvali qo'shildi (`order_counters` TEGILMADI — ishlayotgan kodni qayta yozish ROADMAP §0 da taqiqlangan); MUTATSIYA bilan tasdiqlandi: haydovchi filtri olib tashlansa va `FAILED` buyurtmani bekor qiladigan qilinsa, ikkita test aynan shu ikkisini tutadi; TOZALASH TARTIBI: `Delivery.orderId` da `onDelete: Restrict` (yetkazmasi bor buyurtmani o'chirib bo'lmaydi — tovar qayerga ketgani yo'qolardi), shuning uchun `orders` va `admin-orders` to'plamlarining tozalashi yangilandi — bu S32 da o'lchab aniqlandi; testlar 461 ta
```
