import type { StoreLocation } from '@/lib/types';

/**
 * SOTUV NUQTALARI.
 *
 * BO'SH QOLADI. Spec aniq yozadi: "mijoz bergan sotuv nuqtalari" — do'kon nomi,
 * manzili yoki telefoni O'YLAB TOPILMAYDI (8-qoida). Ro'yxat bo'sh bo'lsa
 * `WhereToBuy` bo'limi CTA va `[CLIENT CONTENT REQUIRED]` bilan ko'rinadi.
 *
 * [CLIENT CONTENT REQUIRED] — sotuv nuqtalari ro'yxati.
 */
export const stores: StoreLocation[] = [];
